# Template — copy to secrets.local.yaml (gitignored) or use kubectl create secret.
# NEVER commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: knowledge-index-secrets
  namespace: knowledge-airflow
type: Opaque
stringData:
  UPSTASH_VECTOR_REST_URL: ""
  UPSTASH_VECTOR_REST_TOKEN: ""
